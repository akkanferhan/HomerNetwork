import Foundation

/// Module namespace and version metadata for HomerNetwork.
public enum HomerNetwork {
    /// The semantic version string of the linked HomerNetwork build.
    public static let version = "0.1.0"
}
